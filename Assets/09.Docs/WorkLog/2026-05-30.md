# 2026-05-30 작업일지

## Phase 4: UI/UX 폴리싱 + 데이터 풀 확충

### Phase 4A: 버그 수정
- TopBarUI `_turnCounterText` 와이어링 수정 (BattleUISceneBuilder.Setup.cs)
- BattleLogUI `_logText` 자동 연결 추가 (BattleUISceneBuilder.Setup.cs)
- ActionBarUI 미사용 필드 `_effectsContainer`, `_targetTypeText` 제거
- MapScene CanvasScaler 설정 (ScaleWithScreenSize, 1920x1080, match=0.5)

### Phase 4B: 사망 상태 + 핵심 데이터
- PlayerSidebarPanel/EnemyDetailPanel에 CanvasGroup + SetDead() 추가
- BattleUIManager.UpdateAllPanels에서 사망 상태 동기화
- CharacterTable.csv 스탯 설계값으로 스케일업 (Warrior HP 20→100 등)
- SkillTable.csv 파워 비례 스케일업 (5~8배)
- StatusEffectType.Taunt 추가 → 도발 스킬 작동
- SkillType.Purify 추가 → 정화 스킬 작동
- EnemyAIController Taunt 타겟 우선 선택 로직
- BattleDisplayUtil Taunt/Purify 라벨 추가
- ActionSlotUI Shield/Purify 색상 추가

### Phase 4C: 적/이벤트 데이터 확충
- 엘리트 적 3종 추가 (정예 기사, 마법사 고블린, 암흑 슬라임)
- 보스 적 3종 추가 (고블린 왕, 드래곤, 마왕)
- CharacterTable.csv, SkillTable.csv, EnemyPatternTable.csv 업데이트
- MapSceneBuilder 적 풀 prefix 기반 분리 (normal/elite/boss)
- 층별 적 스케일링 (Floor 1: 1.0x, 2: 1.3x, 3: 1.6x)
- Character.ApplyFloorScaling() 메서드
- 이벤트 6종 추가 (고대 도서관, 요정의 샘, 쓰러진 기사, 보물 고블린, 저주받은 제단, 상인 대행)
- ItemEffectApplier 순수 C# static 클래스 (아이템 효과 런타임 적용)
- StatComponent.AddPermanentBase() 추가
- GameRunState.AcquireItem에서 즉시 효과 적용

### Phase 4D: UI 폴리싱 기반 시스템
- SceneTransition (페이드 인/아웃, DontDestroyOnLoad)
- ToastUI (화면 상단 토스트 알림, 큐 기반)
- BattleEndOverlay (승리/패배 오버레이 + 계속하기 버튼)
- BattleSceneSetup → 3초 대기 제거, 오버레이 버튼으로 전환
- MapSceneSetup → 페이드 트랜지션 적용

### Phase 4E: UI 디테일
- FloatingTextUI (데미지/힐 플로팅 텍스트)
- UIAnimationHelper (FadeIn/FadeOut/ScaleFromZero)
- ConfirmationDialog (범용 예/아니오 다이얼로그)
- 캠프파이어 휴식 시 토스트 메시지 표시

### 파일 변경 요약
- 수정: 18개 파일
- 신규: 6개 파일 (ItemEffectApplier, SceneTransition, ToastUI, BattleEndOverlay, FloatingTextUI, UIAnimationHelper, ConfirmationDialog)

---

## 가비지 컬렉터 (심층 수집)

### 삭제 완료 (분류 A: 즉시 삭제 8건)
- `FloatingTextUI._prefab` 미사용 필드
- `SkillInventoryComponent.OnSkillDrawn` 미구독 이벤트 + Invoke 2건
- `HealthComponent.HPPercent` 미사용 프로퍼티
- `StatComponent.OnStatChanged` 미구독 이벤트 + Invoke 3건
- `StatusEffectComponent.OnEffectChanged` 미구독 이벤트 + Invoke 2건
- `GameRunState.GetPartyHealthRatio()` 미사용 메서드
- `SceneTransition.FadeOut()`/`FadeIn()` standalone 메서드
- `EnemyActionPattern.PeekNextSkill()` 미사용 메서드

### 보류 항목 (추후 처리 예정)

**분류 B: 죽은 클래스 (연동 필요)**
| 파일 | 설명 | 처리 방안 |
|------|------|-----------|
| FloatingTextUI.cs | BattleUIManager에 연동 안 됨 | Phase 5: FloatingTextUI 연동 |
| UIAnimationHelper.cs | 호출처 없음 | Phase 5: RewardUI/ShopUI/EventUI에 적용 |
| ConfirmationDialog.cs | 인스턴스화된 적 없음 | Phase 5: ShopUI 구매 확인에 적용 |
| RewardData.cs | .asset 없고 RewardManager가 동적 객체 사용 | 검토 후 삭제 또는 RewardManager 리팩터 |

**분류 C: 미사용 API + 아키텍처 이슈**
| 항목 | 파일 | 설명 |
|------|------|------|
| DrawnSkill/RemoveSkill/ClearDrawnSkill | SkillInventoryComponent | 미사용, 향후 스킬 제거 기능 시 필요 가능 |
| OnDeath 이벤트 | HealthComponent | 구독자 없음, 사망 애니메이션 시 필요 가능 |
| GetRemainingDuration | StatusEffectComponent | 호출처 없음, UI 표시 시 필요 가능 |
| Stacks 필드 (항상 1) | StatusEffectComponent.ActiveEffect | 중첩 메커니즘 구현 시 필요 |
| 에디터 색상 상수 중복 | 3개 Editor 파일 | 공유 상수 클래스로 추출 권장 |
| 스킬 라우팅 중복 | EnemyAIController vs TurnManager | switch-case 로직 중복 |
| 하드코딩 폴백 스탯 | BattleSceneSetup:78-92 | 테스트 모드, 데이터 없을 때만 사용 |
| 미구현 ItemEffectType | ItemData.cs | HealPerTurn/ExtraGold/DrawWeightBonus 처리 없음 |
| 순수 C# 클래스 UnityEngine 의존 | 5개 파일 | System.Math/System.Random으로 교체 권장 |

**분류 D: 보류 (기능 구현 시까지)**
| 항목 | 파일 | 설명 |
|------|------|------|
| OnGuardianClicked 스텁 | EnemyDetailPanel:160 | TODO: 가디언 액션 |
| OnArcanaClicked 스텁 | EnemyDetailPanel:165 | TODO: 아크카 액션 |

### 맵 생성 버그 수정
- MapGenerator.ConnectLayers: 모든 하위 노드가 최소 1개 상위 노드와 연결되도록 보장 (막힘 방지)
