# 작업일지 — 2026-05-19

## 세션 1 — 프로젝트 감사 및 하네스(Harness) 효과 검증

### 프로젝트 구조 분석 및 현황 보고
- **상태**: 완료
- **내용**:
  - 전체 프로젝트 구조 탐색 (58개 C# 스크립트, 40개 SO 에셋, 4개 씬)
  - Phase 1-2 (코어 전투) 완료, Phase 3 (로그라이크) 코드 완료 / 통합 진행 중
  - AI Harness 기반 개발 방법론 분석 (CLAUDE.md 가드레일, GC 시스템, 프롬프트 템플릿)

### 하네스 가드레일 및 코드 품질 감사
- **상태**: 완료
- **관련 파일**: 전체 `Assets/02.Scripts/` 대상
- **내용**:
  - 3개 병렬 에이전트로 가드레일 / 아키텍처 / 데드코드 감사 실행
  - **가드레일 결과**: 8개 중 6개 통과
    - 통과: FindObjectOfType 금지, PlayerPrefs 금지, MonoBehaviour 최소화, 네임스페이스 규칙, UI-로직 분리, SO 데이터 관리
    - 미흡: private 필드 네이밍 (_underscoreCamelCase vs 문서상 camelCase 불일치), 일부 미사용 코드
  - **HIGH 버그 4건 발견**:

| ID | 심각도 | 내용 | 위치 |
|----|--------|------|------|
| H1 | HIGH | `ApplyStatModifiers()`가 어디서도 호출되지 않음 — 스탯 modifier가 실제 스탯에 반영 안 됨 | Character.cs, TurnManager.cs |
| H2 | HIGH | `OnTurnEnd()`가 어디서도 호출되지 않음 — 상태이상 지속시간 감소/DoT 데미지 미처리 | TurnManager.cs |
| H3 | HIGH | `EnemyAIController`가 생성만 되고 실행되지 않음 — 적 턴이 raw 공격만 사용 | TurnManager.cs, BattleSceneSetup.cs |
| H4 | HIGH | MapScene에서 하드코딩된 적 스탯 — ScriptableObject 풀 미사용 | MapSceneSetup.cs |

---

## 세션 2 — HIGH 버그 수정 (H1 ~ H4)

### H1+H2+H3: 전투 사이클 핵심 수정
- **상태**: 완료
- **관련 파일**:
  - `Assets/02.Scripts/Combat/Turn/TurnManager.cs`
  - `Assets/02.Scripts/Combat/AI/EnemyAIController.cs`
  - `Assets/02.Scripts/Combat/BattleSceneSetup.cs`
- **내용**:
  - **TurnManager.cs**:
    - `using TeamLog.Combat.AI` 추가, `_enemyControllers` 필드 추가
    - 생성자 시그니처 변경: `List<EnemyAIController> enemyControllers = null` 파라미터 추가
    - `CalculateDamage(int attackPower, int defense)` static 메서드 추가 — 데미지 공식 중앙화 (`Max(1, atk - def)`)
    - `StartNewTurn()`: 턴 시작 시 모든 캐릭터 `ApplyStatModifiers()` 호출 추가
    - `ExecuteEnemyActions()`: raw 공격 → `_enemyControllers` 기반 AI 실행으로 전면 교체
    - `ProcessTurnEnd()`: 모든 캐릭터 `OnTurnEnd()` + `ApplyStatModifiers()` 처리 추가
    - `ApplyEffect()`: 효과 적용 후 `ApplyStatModifiers()` 호출 추가
  - **EnemyAIController.cs**:
    - `using TeamLog.Combat.Turn` 추가
    - `AttackTarget()`: `TurnManager.CalculateDamage()` 사용으로 통일
    - `ApplyDefenseBuff()`, `ApplySelfBuff()`, `ApplyDebuff()`: `ApplyStatModifiers()` 호출 추가
  - **BattleSceneSetup.cs**:
    - 적 AI 컨트롤러 생성을 TurnManager 인스턴스화 이전으로 이동
    - `_enemyControllers`를 TurnManager 생성자에 전달
- **결과**: 스탯 modifier 정상 반영, 상태이상 턴 처리 정상, 적 AI 패턴 실행 확인

### H4: 적 데이터 하드코딩 제거
- **상태**: 완료
- **관련 파일**:
  - `Assets/02.Scripts/UI/Map/MapSceneSetup.cs`
  - `Assets/02.Scripts/Editor/MapSceneBuilder.cs`
- **내용**:
  - MapSceneSetup에 `_normalEnemyPool`, `_eliteEnemyPool`, `_bossEnemyPool` SerializeField 배열 추가
  - `StartBattle()`: 하드코딩 스탯 제거, 풀에서 랜덤 선택으로 변경
  - `EnsureEnemyPoolsLoaded()`: 풀 비어 있을 시 경고 로그
  - MapSceneBuilder: `Enemy_` 접두사 CharacterData 에셋 자동 로딩 후 3개 풀에 할당
- **결과**: 에셋 기반 데이터 주입, MapScene 리빌드 시 자동 연결

### GC: 미사용 using 제거
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/Combat/AI/EnemyAIController.cs`
- **내용**: 사용되지 않는 type alias (CharacterData, SkillData, SkillType) 제거

---

## 세션 3 — MapScene UI 및 전투 전환 수정

### MapScene 맵 노드 하단 잘림 수정
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/UI/Map/MapView.cs`
- **내용**:
  - 원인: `CreateNodeButtons()`에서 노드 y좌표가 `y = -layer * spacing`으로 항상 아래로만 이동 → 하단 노드가 컨테이너 밖으로 벗어남
  - 수정:
    - 전체 레이어 높이 계산 후 `startY = totalHeight / 2f`로 상하 대칭 배치
    - 컨테이너 높이 초과 시 자동 간격 축소 (`containerHeight * 0.9f / (layerCount - 1)`)
- **결과**: 모든 층의 노드가 화면 내에 정상 표시

### MapScene → BattleScene 전투 전환 수정
- **상태**: 완료
- **관련 파일**:
  - `Assets/02.Scripts/UI/Map/MapSceneSetup.cs`
  - `Assets/02.Scripts/Editor/MapSceneBuilder.cs`
- **내용**:
  - 원인: MCP Unity `update_component`로 배열 필드(_normalEnemyPool 등) 할당 시 실제 반영 안 됨
  - 1차 시도: `Resources.LoadAll` 폴백 → 에셋이 Resources 폴더가 아니라 불가
  - 최종 해결: MapSceneBuilder에 적 풀 자동 로딩 로직 추가 후 씬 리빌드
    - `LoadAllAssets<CharacterData>(CHAR_DIR).FindAll(a => a.name.StartsWith("Enemy_"))` 로 Enemy_Goblin, Enemy_Slime 확보
    - `_normalEnemyPool`, `_eliteEnemyPool`, `_bossEnemyPool` 3개 배열에 자동 할당
  - MapSceneSetup.EnsureEnemyPoolsLoaded()를 경고 로그만 남기도록 단순화
- **결과**: MapSceneBuilder 리빌드 후 적 풀 2개 에셋 포함, 전투 노드 클릭 → BattleScene 전환 성공

### StatComponent 컬렉션 수정 예외 수정
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/Characters/Components/StatComponent.cs`
- **내용**:
  - 원인: `ClearModifiers()`에서 `foreach (var key in _modifiers.Keys)` 순회 중 `_modifiers[key] = 0` 값 수정 → `InvalidOperationException`
  - 수정: `new List<StatType>(_modifiers.Keys)`로 키를 복사한 뒤 순회
- **스택 트레이스**:
  ```
  StatComponent.ClearModifiers() → Character.ApplyStatModifiers()
  → TurnManager.StartNewTurn() → TurnManager.StartBattle()
  → BattleSceneSetup.InitializeBattle() → BattleSceneSetup.Start()
  ```
- **결과**: 전투 시작 시 예외 제거

### BattleUI 한국어 폰트 누락 수정
- **상태**: 완료 (코드 수정 완료, 씬 리빌드 필요)
- **관련 파일**: `Assets/02.Scripts/Editor/BattleUISceneBuilder.cs`
- **내용**:
  - 원인: 5곳의 `AddComponent<TextMeshProUGUI>()`에서 `tt.font = GetOrCreateKoreanFont()` 할당 누락
  - 수정 위치:
    - `CurrentTurnText` (하단바 턴 표시)
    - 카운터 텍스트 (슬롯 카운트)
    - 턴 종료 버튼 텍스트 (`"턴 종료 [T]"`)
    - 로그 텍스트 (전투 로그)
    - `AddText()` 헬퍼 메서드 (공용)
  - 플레이 모드 정지 후 `TeamLog > Scene > Build Battle UI Scene` 리빌드 필요
- **결과**: 한국어 폰트 미지원 경고 해결 예정

---

---

## 세션 4 — Battle↔Map 런 상태 유지 및 Battle→Reward 연결 (5/21)

### GameRunState 싱글톤화
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/Map/GameRunState.cs`
- **내용**:
  - 기존: 매 씬마다 새 인스턴스 생성 → 씬 전환 시 런 상태 초기화됨
  - `static Instance` 싱글톤으로 변경, 씬 전환 시에도 런 상태 유지
  - `Create()` / `Destroy()` / `Cleanup()` 정적 메서드 추가
  - 생성자 `private`화로 외부 new 금지
  - `PeekRandomSkill()` / `PeekRandomItem()` 추가 (보상 미리보기용, 실제 획득은 ClaimReward에서)
- **결과**: Battle→Map 복귀 시 골드, 파티 HP, 층 진행도 유지

### BattleResult 전투 결과 전달 시스템
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/Combat/BattleResult.cs` (신규)
- **내용**:
  - 정적 클래스로 전투 결과를 씬 전환 간 전달
  - `SetBattleType(MapNodeType)`: MapSceneSetup에서 전투 시작 전 노드 타입 설정
  - `SetResult(bool victory)`: BattleSceneSetup에서 전투 종료 시 결과 기록
  - `Clear()`: MapSceneSetup에서 보상 처리 완료 후 초기화
- **결과**: 전투 승리/패배 결과가 MapScene에 정상 전달

### MapSceneSetup 씬 복귀 및 보상 흐름
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/UI/Map/MapSceneSetup.cs`
- **내용**:
  - `RestoreExistingRun()`: 씬 복귀 시 기존 런 인스턴스 복원, 이벤트 재구독, MapView 복원
  - `HandleBattleResult()`: 승리 시 `OnBattleVictory()` → `_rewardUI.ShowRewards()`, 패배 시 `EndRunDefeat()`
  - `InitializeSubUIs()`: 서브 UI 초기화 로직 공통 추출
  - 보스 클리어 → 다음 층 이동을 보상 선택 이후(`OnRewardComplete`)로 이동
  - `StartBattle()`에 `BattleResult.SetBattleType()` 추가
- **결과**: Map→Battle→승리→Reward→Map 전체 사이클 동작

### RewardManager 지연 획득 패턴
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/Reward/RewardManager.cs`
- **내용**:
  - `GenerateRewards()`: `AcquireRandomSkill/Item` → `PeekRandomSkill/Item` 변경 (미리보기만)
  - `ClaimReward()`: 선택 시점에 `runState.AcquireSkill/Item` 실제 획득
  - 기존: 보상 생성 시 즉시 획득 → 취소 불가
  - 변경: 보상 미리보기 → 플레이어 선택 시 획득 (지연 획득)
- **결과**: 보상 선택 전 인벤토리에 추가되지 않음

### ActionSlotUI 시전자 이름 표시
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/UI/Battle/ActionSlotUI.cs`
- **내용**:
  - `_casterNameText` 필드 추가
  - `Setup()`에서 시전자 이름 표시, `Clear()`에서 초기화
- **결과**: 액션 슬롯에 어떤 캐릭터의 스킬인지 표시

### BattleUI 씬 삭제 (BattleScene에 통합)
- **상태**: 완료
- **관련 파일**: `Assets/01.Scenes/BattleUI.unity` (삭제)
- **내용**: BattleUI 씬이 BattleScene에 통합되어 개별 씬 불필요

---

## 플레이 테스트 결과 (5/22 검증 완료)

| 항목 | 결과 |
|------|------|
| MapScene 렌더링 | 정상 (상단 패널, 노드 배치) |
| 맵 노드 하단 잘림 | 수정 완료 |
| 전투 노드 → BattleScene 전환 | 성공 |
| 전투 시작 시 StatComponent 예외 | 수정 완료 |
| 한국어 폰트 경고 | 수정 완료 |
| BattleScene 내 전투 사이클 | 검증 완료 |
| Battle↔Map 런 상태 유지 | 검증 완료 |
| Battle→Reward 연결 | 검증 완료 |

---

## 수정 파일 총합

| 파일 | 수정 내용 |
|------|-----------|
| `Combat/Turn/TurnManager.cs` | AI 연동, 데미지 공식 중앙화, 턴 종료 처리 |
| `Combat/AI/EnemyAIController.cs` | CalculateDamage 사용, ApplyStatModifiers 호출, 미사용 using 제거 |
| `Combat/BattleSceneSetup.cs` | AI 컨트롤러 생성 순서 변경, BattleResult.SetResult 추가 |
| `Combat/BattleResult.cs` | 신규: 전투 결과 씬 전환 간 전달 |
| `UI/Map/MapSceneSetup.cs` | 적 데이터 풀, 런 복원, 전투 결과 처리, 보상 흐름 |
| `UI/Map/MapView.cs` | 노드 수직 중앙 정렬, 자동 간격 조정 |
| `Map/GameRunState.cs` | 싱글톤화, Peek 메서드, Cleanup |
| `Reward/RewardManager.cs` | 지연 획득 패턴 (Peek → Claim) |
| `UI/Battle/ActionSlotUI.cs` | 시전자 이름 표시 필드 추가 |
| `Editor/MapSceneBuilder.cs` | 적 풀 자동 로딩 로직 |
| `Editor/BattleUISceneBuilder.cs` | 한국어 폰트 5곳 누락 수정 |
| `Characters/Components/StatComponent.cs` | ClearModifiers 컬렉션 수정 예외 수정 |
| `01.Scenes/BattleUI.unity` | 삭제 (BattleScene에 통합) |

---

## 다음 작업 후보

| 우선순위 | 작업 | 상태 |
|----------|------|------|
| 1 | 테스트용 EventData, ItemData SO 에셋 생성 | 필요 |
| 2 | 심층 가비지 컬렉터 (Phase 3→4 전환) | 권장 |
| 3 | Phase 4: 폴리싱 (사운드, 이펙트, 밸런싱) | 미착수 |
