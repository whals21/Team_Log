# 작업일지 — 2026-04-13

## Phase 3 프리팹 생성 및 MapScene 런타임 디버깅

### Phase3PrefabBuilder 에디터 도구 생성
- **상태**: 완료
- **관련 파일**:
  - `Assets/02.Scripts/Editor/Phase3PrefabBuilder.cs` — 프리팹 일괄 생성 에디터
  - `Assets/03.Data/Prefabs/MapNodeButton.prefab`
  - `Assets/03.Data/Prefabs/MapConnectionLine.prefab`
  - `Assets/03.Data/Prefabs/MapPlayerMarker.prefab`
  - `Assets/03.Data/Prefabs/RewardCard.prefab`
  - `Assets/03.Data/Prefabs/ShopItemSlot.prefab`
  - `Assets/03.Data/Prefabs/ChoiceButton.prefab`
- **내용**:
  - TeamLog > Prefabs > Create Phase 3 Prefabs 메뉴로 6개 프리팹 일괄 생성
  - 리플렉션 기반 SerializeField 자동 연결 (Wire 메서드)
  - NanumGothic SDF 한국어 폰트 자동 적용
  - 다크 테마 색상 팔레트 통일 (BgDark, BgPanel, AccentYellow 등)
- **결과**: 프리팹 6개 생성 완료

### 컴파일 에러 수정 (4건)
- **상태**: 완료
- **내용**:
  1. `MapFloor.cs` — IReadOnlyList.Contains 오류 → `using System.Linq` 추가
  2. `MapSceneSetup.cs` — BattleSceneSetup 미인식 오류 → `using TeamLog.Combat` 추가
  3. `Phase3PrefabBuilder.cs` — TextAlignmentOptions.MiddleRight 미존재 → `TextAlignmentOptions.Right`로 변경
  4. `Phase3PrefabBuilder.cs` — ChoiceButton NullReferenceException → TMP를 Image와 분리하여 자식 오브젝트로 이동 (Image와 TextMeshProUGUI 같은 GameObject 불가)
- **결과**: 전체 컴파일 0 errors

### MapScene 씬 빌드 및 인스펙터 연결
- **상태**: 완료
- **관련 파일**:
  - `Assets/01.Scenes/MapScene.unity`
  - `Assets/02.Scripts/Editor/MapSceneBuilder.cs`
- **내용**:
  - MapSceneBuilder로 씬 생성 (Canvas, TopPanel, NodeContainer, LineContainer)
  - MCP Unity로 프리팹을 MapView SerializeField에 연결
    - `_nodeButtonPrefab` → MapNodeButton.prefab
    - `_connectionLinePrefab` → MapConnectionLine.prefab
    - `_playerMarkerPrefab` → MapPlayerMarker.prefab
  - MapSceneSetup에 CharacterData 4종 연결
    - `_testWarriorData` → Char_Warrior.asset
    - `_testMageData` → Char_Mage.asset
    - `_testHealerData` → Char_Healer.asset
    - `_testRogueData` → Char_Rogue.asset
  - BuildSettings에 MapScene, BattleScene, BattleUI 등록

### MapScene 런타임 이슈 수정
- **상태**: 완료
- **내용**:
  1. **"No cameras rendering" 문제**
     - 원인: MapSceneBuilder에 카메라 생성 로직 누락
     - 조치: MapSceneBuilder에 Camera + EventSystem 생성 코드 추가
     - MCP Unity로 Main Camera (Camera 컴포넌트, MainCamera 태그) 추가
     - MCP Unity로 EventSystem (EventSystem + StandaloneInputModule) 추가
  2. **맵 노드 미표시 문제**
     - 원인: MapView의 프리팹 SerializeField 3개가 None (미연결)
     - 조치: MCP Unity로 프리팹 에셋 연결
  3. **CharacterData 미연결 문제**
     - 원인: MapSceneSetup의 테스트 캐릭터 데이터 4개가 null
     - 조치: MCP Unity로 CharacterData 에셋 4개 연결
- **결과**: MapScene 런타임에서 상단 패널(층/골드) 정상 표시 확인, 맵 노드 표시는 프리팹 연결 후 재확인 필요

### MapSceneBuilder 코드 개선
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/Editor/MapSceneBuilder.cs`
- **내용**:
  - 카메라 생성 로직 추가 (Main Camera + Camera 컴포넌트 + MainCamera 태그)
  - EventSystem 생성 로직 추가 (EventSystem + StandaloneInputModule)
  - 프리팹 3종 자동 로딩 및 MapView SerializeField 연결 (AssetDatabase.LoadAssetAtPath + SerializedObject)
  - CharacterData 4종 자동 로딩 및 MapSceneSetup SerializeField 연결
  - 향후 재빌드 시 수동 작업 불필요
- **결과**: Build Map Scene 한 번에 전체 씬 구성 완료

### MapNodeReference 스크립트 분리 및 프리팹 재생성
- **상태**: 완료
- **관련 파일**:
  - `Assets/02.Scripts/UI/Map/MapNodeReference.cs` — 새 파일 (MapView.cs에서 분리)
  - `Assets/02.Scripts/UI/Map/MapView.cs` — MapNodeReference 클래스 제거
  - `Assets/03.Data/Prefabs/*.prefab` — 기존 6개 삭제 후 재생성
- **내용**:
  - 원인: MapNodeReference가 MapView.cs 내부에 정의되어 Unity가 스크립트 참조를 직렬화하지 못함 (`m_Script: {fileID: 0}`)
  - "The referenced script on this Behaviour (Game Object 'MapNodeButton') is missing!" 경고 15건 발생
  - 조치: MapNodeReference를 별도 파일로 분리 (Unity는 MonoBehaviour 파일명=클래스명 요구)
  - 기존 프리팹 6개 삭제, Phase3PrefabBuilder로 재생성
  - MapSceneBuilder로 씬 재빌드
- **결과**: MapScene 런타임에서 이벤트 노드, 상점 노드 정상 동작 확인

---

## 세션 2 — BattleScene 통합 및 전투 흐름 수정

### 씬 파일 Missing Script 경고 정리
- **상태**: 완료
- **관련 파일**:
  - `Assets/01.Scenes/BattleScene.unity`
  - `Assets/01.Scenes/BattleUI.unity`
  - `Assets/01.Scenes/TestCombatScene.unity`
- **내용**:
  - 가비지 컬렉터에서 삭제된 스크립트 7개의 MonoBehaviour 참조가 3개 씬에 잔존
  - Python 스크립트로 YAML에서 해당 MonoBehaviour 블록 및 컴포넌트 참조 제거
  - 대상: TestCombatManager, Main Camera 커스텀 스크립트, EventSystem 커스텀 스크립트, Global Light 2D 스크립트, BattleUIRoot 커스텀 스크립트
- **결과**: 콘솔 경고 0건, 0 에러

### BattleScene에 전투 UI 직접 구축
- **상태**: 완료
- **관련 파일**:
  - `Assets/02.Scripts/Editor/BattleUISceneBuilder.cs` — `BuildBattleScene()` 메서드 추가
  - `Assets/01.Scenes/BattleScene.unity`
- **내용**:
  - 기존: BattleScene에는 배경만, 전투 UI는 별도 BattleUI 씬에 존재
  - 방식 비교 후 **방식 1 (UI를 BattleScene에 직접 빌드)** 선택
  - `BattleUISceneBuilder.BuildBattleScene()` 추가:
    - 기존 BattleScene 로드 → TestCombatManager/BattleUICanvas 제거
    - Canvas + BattleUIRoot + TopBar/BottomBar/LeftSidebar/RightSidebar/CenterArea 생성
    - `SetupScriptsInCurrentScene()`으로 BattleUIManager, ActionBarUI, TopBarUI 등 컴포넌트 추가
    - SerializeField 리플렉션 자동 연결
  - `Tools > Battle UI > Build Battle Scene (with BG)` 메뉴로 실행
- **결과**: BattleScene에 완전한 전투 UI 구축, BattleSceneSetup에 CharacterData 4종 + EnemyData 연결 완료

### 패널 중복 버그 수정
- **상태**: 완료
- **관련 파일**:
  - `Assets/02.Scripts/Editor/BattleUISceneBuilder.cs` — `RemoveStaticPanels()` 추가
  - `Assets/02.Scripts/UI/Battle/BattleUIManager.cs` — `ClearContainerChildren()` 추가
- **내용**:
  - 원인: 빌더가 정적 패널(4개 플레이어 + 2개 적) 생성 → 런타임에도 프리팹으로 동적 생성 → 중복 표시
  - 수정 1: `SetupScriptsInCurrentScene()`에서 프리팹 생성 후 정적 패널 제거 (`RemoveStaticPanels`)
  - 수정 2: `BattleUIManager.CreatePlayerPanels()` / `CreateEnemyPanels()`에 `ClearContainerChildren()` 안전망 추가
- **결과**: LeftSidebar/CenterArea childCount=0 확인, 런타임에만 동적 패널 생성

### 턴 사이클 자동 진행 수정
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/Combat/Turn/TurnManager.cs`
- **내용**:
  - 원인: `ConfirmActions()`이 Execution 페이즈만 설정하고 `ExecuteActions()` / `StartEnemyTurn()` 미호출 → 턴 종료 후 아무 일도 안 일어남
  - 수정: `ConfirmActions()`에서 `ExecuteActions()` → `StartEnemyTurn()` 자동 체인
  - 이후 즉시 시전 방식으로 변경하면서 `ConfirmActions()`은 적 턴 전환만 담당하도록 간소화
- **결과**: 턴 종료 → 적 턴 자동 진행 → 다음 턴 드로우 정상 동작

### 스킬 즉시 시전으로 전투 흐름 변경
- **상태**: 완료
- **관련 파일**:
  - `Assets/02.Scripts/Combat/Turn/TurnManager.cs` — `ExecuteSkillImmediately()` 추가
  - `Assets/02.Scripts/Combat/PlayerActionController.cs` — `CastImmediately()` 추가, `AssignTarget()`/`UnassignSlot()` 제거
- **내용**:
  - 기존: 스킬 할당 → 턴 종료 시 일괄 실행 (Slay the Spire식)
  - 변경: 스킬 슬롯 클릭 → 대상 클릭 → **즉시 시전**
  - TurnManager: `ExecuteSkillImmediately(caster, skill, target)` 추가 (단일/범위 모두 지원)
  - PlayerActionController: `AssignTarget()` → `CastImmediately()`로 대체
  - Self/AllEnemies/AllAllies: 대상 선택 없이 즉시 시전
  - SingleEnemy/SingleAlly: 대상 하이라이트 후 클릭 시 즉시 시전
  - 모든 스킬 사용 완료 시 자동으로 적 턴 전환
  - 턴 종료 버튼: 남은 스킬 무시하고 적 턴으로 즉시 전환
- **결과**: 스킬 즉시 시전, 적 턴 자동 진행 확인

### 전투 종료 후 MapScene 복귀
- **상태**: 완료
- **관련 파일**: `Assets/02.Scripts/Combat/BattleSceneSetup.cs`
- **내용**:
  - 원인: `OnBattleEnded()`가 로그만 출력하고 씬 전환 없음
  - 수정: `BattleEndTransition()` 코루틴 추가 — 3초 대기 후 `SceneManager.LoadScene("MapScene")`
  - `using UnityEngine.SceneManagement`, `System.Collections` 추가
- **결과**: 적 전멸 시 "전투 승리!" 로그 → 3초 후 MapScene 자동 복귀

### 플레이 테스트 결과
- 스킬 즉시 시전: 정상 동작
- 적 전멸 시 MapScene 복귀: 정상 동작
- 알려진 이슈: MapScene 복귀 시 GameRunState 초기화됨 (런 상태 유지 불가)

## 커밋
- (이번 세션 작업은 아직 커밋 전)

## 다음 작업 후보

| 우선순위 | 작업 | 상태 |
|----------|------|------|
| 1 | Battle↔Map 런 상태 유지 (GameRunState 보존) | 필요 |
| 2 | Battle→Reward 연결 (전투 승리 후 보상 흐름) | 필요 |
| 3 | 테스트용 EventData, ItemData SO 에셋 생성 | 필요 |
| 4 | 심층 가비지 컬렉터 (Phase 3→4 전환) | 권장 |
| 5 | Phase 4: 폴리싱 (사운드, 이펙트, 밸런싱) | 미착수 |
