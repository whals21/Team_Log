# 2026-06-06 작업일지: Phase 4L-4Q + Phase 5A-5D + 유물 바 전투 씬 추가

## 유물 바 전투 씬 추가 (세션 2)

### 문제
맵 씬 하단에 표시되는 유물(RelicBarUI)이 전투 씬에서는 확인 불가. 플레이어가 전투 중 자신이 보유한 유물을 볼 수 없음.

### 신규 파일
1. `UI/Battle/BattleRelicBarUI.cs` — 전투 씬용 유물 바 컴포넌트
   - `GameRunState.Instance?.RelicHandler`에서 자동 읽기
   - 유물 아이콘 + TooltipTarget(이름/설명 툴팁) 표시
   - 테스트 모드(GameRunState 없음)에서는 빈 상태로 안전 동작

### 수정 파일
1. `Combat/BattleSceneSetup.cs` — `_relicBarUI` 필드 추가, 전투 시작 시 `Refresh()` 호출
2. `Editor/BattleUISceneBuilder.UI.cs` — `CreateRelicBar()` 메서드 추가 (BottomBar 내부 우측), `CreateBottomBar()`에서 호출
3. `Editor/BattleUISceneBuilder.Setup.cs` — BattleRelicBarUI를 BattleSceneSetup에 자동 와이어링

### 배치 위치
- BottomBar 좌측(턴 텍스트 우측) 영역
- HorizontalLayoutGroup으로 유물 아이콘 나열
- 아이콘 크기 36×36 (맵 씬 48×48보다 작게, 전투 UI 공간 절약)
- 카운트 라벨 "유물 N" 표시

### 컴파일 결과
0 에러, 15 경고 (기존과 동일)

### 씬 리빌드
- BattleScene: Build Battle Scene (with BG) → 정상
- MapScene: Build Map Scene → 유물 풀 12개 로드 확인

---

## Phase 5B: 스킬 성장 시스템

### 신규 파일
1. `Skill/SkillInstance.cs` — SkillData(SO) + UpgradeLevel 래퍼, EffectivePower/Cost/Weight 계산 프로퍼티

### 수정 파일
1. `SkillInventoryComponent.cs` — List<SkillData> → List<SkillInstance>, DrawSkillInstance(), FindInstance()
2. `SkillDrawSystem.cs` — DrawnSkillSlot이 SkillInstance 참조, SetInstance() 추가
3. `TurnManager.cs` — ExecuteSkillImmediately에 bonusPower 오버로드, ExecuteAttack/Heal/Shield에 bonusPower 반영
4. `PlayerActionController.cs` — SkillInstance.EffectivePower/Cost로 bonusPower 계산 후 전달
5. `SkillUpgradeUI.cs` — 휴식지 스킬 강화 UI (파티 전체 업그레이드 가능 스킬 표시)
6. `SaveManager.cs` — CharacterSaveData에 SkillUpgradeLevels 추가, 저장/로드

## Phase 5C: 유물(Relic) 시스템

### 신규 파일
1. `Combat/CombatEventBus.cs` — static 전투 이벤트 버스 (11개 이벤트: BattleStart/End, TurnStart/End, DamageDealt/Received, Kill, HealApplied, ShieldGained, GoldEarned, SkillUsed)
2. `Reward/RelicData.cs` — 유물 SO (RelicTrigger 11종 + RelicEffectType 11종)
3. `Reward/RelicHandler.cs` — CombatEventBus 구독, 트리거 매칭 → 효과 적용, GetBonusOutgoingDamage/GetDamageReduction

### 수정 파일
1. `TurnManager.cs` — StartBattle/StartNewTurn/ProcessTurnEnd/CheckBattleEnd에 CombatEventBus.Fire* 추가, DealDamage에 FireDamageDealt/Received/FireKill
2. `GameRunState.cs` — RelicHandler 프로퍼티, AcquireRelic() 메서드
3. `RewardTypes.cs` — RewardType.Relic 추가
4. `RewardManager.cs` — RewardOffer.Relic 필드, ApplyReward Relic 케이스
5. `DataGenerator.Content.cs` — GenerateRelicData() 12종 유물 생성
6. `DataGenerator.cs` — GenerateAllTestData에 GenerateRelicData 추가

### 유물 12종
불타는 검(+3공격), 철가죽(-2피해), 재생의 반지(턴당+3HP), 황금 부적(+15골드), 방패 부적(+3쉴드), 흡혈 송곳니(처치+5HP), 광전사 인장(처치당+2공격 누적), 네잎클로버(+5가중치), 가시 갑옷(반사2), 질풍 부츠(턴당+2쉴드), 전투 깃발(시작+5쉴드), 치유 허브(시작10%HP)

## Phase 5D: 폴리싱 + 깊이

### 수정 파일
1. `ItemEffectApplier.cs` — HealPerTurn/ExtraGold/DrawWeightBonus 처리 메서드 추가
2. `EventData.cs` — EventOutcome에 ApplyStatusEffect/Duration/Value 필드 추가
3. `EventManager.cs` — ProcessChoice에 상태이상 적용 처리

---

### 완료 항목

#### ✅ 5A: 타이틀 + 런 종료 + 메타프로그레션

**게임 플로우 변화**:
- Before: MapScene 직접 진입 → 런 종료 시 아무 일 없음
- After: TitleScene → NewGame/Continue → MapScene → ... → RunEndOverlay → TitleScene

**신규 파일**:
1. `Map/MetaSaveData.cs` — 런 간 영구 통계 DTO (TotalRuns, Victories, BestFloor, TotalGoldEarned, HasPendingRun)
2. `UI/Title/TitleSceneSetup.cs` — 타이틀 화면 (새 게임/이어하기, 통계 표시)
3. `UI/Map/RunEndOverlay.cs` — 런 종료 오버레이 (승리/패배, 도달 층/골드/전투 승리 수)
4. `01.Scenes/TitleScene.unity` — 타이틀 씬 (MapSceneBuilder.BuildTitleScene)

**수정 파일**:
1. `SaveManager.cs` — MetaSavePath, Meta 프로퍼티, LoadOrCreateMeta(), SaveMeta(), RecordRunEnd() 추가
2. `GameRunState.cs` — BattlesWon/TotalGoldEarned 통계, AdvanceToNextFloor() 자동 저장
3. `MapSceneSetup.cs` — SaveManager.HasSave 분기(ContinueFromSave), OnRunEnded → RunEndOverlay, 자동 저장(휴식/상점/이벤트/보상 후)
4. `MapSceneBuilder.cs` — BuildTitleScene 메뉴, AddSceneToBuildSettings
5. `MapSceneBuilder.Panels.cs` — BuildRunEndOverlay 패널 생성

**자동 저장 타이밍**:
- 층 이동 (AdvanceToNextFloor)
- 휴식/수련/명상 후
- 상점 나가기 후
- 이벤트 완료 후
- 일반 전투 보상 선택 후

---

# 이전 작업: Phase 4L-4Q (사운드/아이콘/VFX/밸런싱/MTP/저장)

## 작업 개요
미사용 에셋 활용 + 잔여 시스템 통합:
1. CombatMagicSpellsVIISFX → 스킬 타입별 전투 사운드 42종 ✅
2. Layer Lab ItemIcons → 아이템 아이콘 시스템 ✅
3. VFXManager → URP Camera Stacking 해결 ✅
4. 밸런싱 패스 ✅
5. Motion Titles Pack → 전투 타이틀 ✅
6. JsonUtility 기반 저장 시스템 ✅

## 완료 항목

### ✅ 4L: 스킬 타입별 전투 사운드 (CombatMagicSpellsVIISFX)

**AudioPalette 확장** (28→42개 매핑):
- FireImpact, IceImpact, ThunderImpact, DarkImpact, PoisonImpact
- BurnImpact, FreezeImpact, HealImpact, BuffCast, DebuffCast
- PurifyCast, ShieldCast, CriticalHit, EnemySkillHit

**TurnManager 이벤트 인프라**:
- `OnSkillApplied` static event — `ExecuteSkillInternal()`에서 스킬+타겟 정보 전달
- `OnAttackMissed` static event — 회피 특성 시 MISS 사운드

**BattleSceneSetup 라우팅**:
- `OnSkillApplied` 구독 → SkillType 분기 (Attack/Heal/Buff/Debuff/Shield/Purify)
- `PlayAttackSound()` → StatusEffectType 기반 세분화 (Burn/Poison/Freeze/일반)
- 기존 `Health.OnDamageTaken += PlayAttackHit` 제거 → 스킬 컨텍스트 기반으로 전환

**AudioManager 편의 메서드**: 14개 추가 (총 40개)

### ✅ 4N: 아이템 아이콘 시스템 (Layer Lab ItemIcons)

**ItemData 확장**:
- `_icon` Sprite 필드 + `Icon` 프로퍼티 추가

**DataGenerator 자동 할당**:
- `GetItemIconPath()` — ItemEffectType 기반 6종 매핑:
  - MaxHPUp → Heart, ATKUp → Sword, DEFUp → Shield
  - HealPerTurn → Potion01_Red, ExtraGold → Gold, DrawWeightBonus → Star
- 폴백: Bag 아이콘

**UI 연동**:
- `ShopItemSlot` — `_iconImage` 필드 추가, `Setup()`에서 `item.Icon` 할당
- `RewardCard` — `_iconImage` 필드 추가, 아이템 보상 시 아이콘 표시

### ✅ 4M: VFXManager URP Camera Stacking

**기존 문제**: ScreenSpaceOverlay 위 ParticleSystem 렌더링 불가 (4가지 방법 시도 후 실패)

**해결**: URP Camera Stacking 방식으로 전환
- Main Camera → Base renderType
- VFX Camera → Overlay renderType, VFX 레이어(30)만 렌더
- `UniversalAdditionalCameraData.cameraStack`에 VFX Camera 추가
- 파티클 → VFX Root에서 스폰 → VFX Camera가 렌더 → Main Camera에 Stacking
- `Unity.RenderPipelines.Universal.Runtime` asmdef 참조 추가

### ✅ 4O: 밸런싱 패스

**CharacterTable.csv 변경**:
- 플레이어 HP 증가: Warrior 100→120, Mage 55→70, Healer 65→80, Rogue 60→75
- 보스 쉴드 너프: GoblinKing 18→12, DemonLord 20→15
- 보스 HP 조정: GoblinKing 150→130, Dragon 200→160, DemonLord 250→200

**SkillTable.csv 변경**:
- 플레이어 힐/쉴드 위력 소폭 하향 (15→12)
- 적 스킬 위력 전반 하향
- Buff 효과값 조정 (AttackUp 5→3-4)

### ✅ 4P: Motion Titles Pack 통합

**BattleTitleManager.cs** (신규):
- `ShowBattleStart()`, `ShowVictory()`, `ShowDefeat()` 메서드
- `StyleManager` 설정 (showFor=2s, auto-play)
- `TextItem`에 한국어 폰트 적용

**asmdef 처리**:
- `MotionTitlesPack.Runtime.asmdef` 생성 (MTP Scripts 폴더)
- `TeamLog.Runtime.asmdef`에 `MotionTitlesPack.Runtime` 참조 추가
- 글로벌 Assembly-CSharp의 타입을 asmdef에서 사용 불가 → asmdef 생성으로 해결

**BattleSceneSetup 연동**:
- `_titleManager` 필드 추가
- 전투 시작 → `ShowBattleStart()`
- 승리 → `ShowVictory()`, 패배 → `ShowDefeat()`

### ✅ 4Q: 저장 시스템 (JsonUtility 기반)

**SaveManager.cs** (신규):
- `RunSaveData` / `CharacterSaveData` DTO (`[Serializable]`)
- `Save()` — 런 상태, 파티 HP/ATK/DEF, 스킬/아이템 경로를 JSON으로 저장
- `Load()` — DTO에서 파티 재구성, HP/스탯/스킬 복원, GameRunState 생성
- `DeleteSave()` — 저장 파일 삭제
- `Application.persistentDataPath/run_save.json` 사용

**ES3 → JsonUtility 전환 이유**:
- ES3 스크립트에 asmdef가 없어 글로벌 Assembly-CSharp에 컴파일됨
- TeamLog.Runtime (asmdef)에서 ES3 타입 접근 불가
- JsonUtility는 UnityEngine 내장 — 의존성 문제 없음
- `ForceKill()` 미존재 → `TakeDamage(MaxHP)`로 사망 처리

**GameRunState 확장**:
- `RestoreBonusAP(int bonus)` — 저장 복원용 BonusAP 설정 메서드

## 주요 기술적 결정

### asmdef 경계 패턴 (3사례)
| 에셋 | 문제 | 해결 |
|------|------|------|
| DOTween | 확장 메서드가 글로벌 어셈블리 | DOTween.To() 직접 사용 |
| Motion Titles Pack | MTP 타입이 글로벌 어셈블리 | MotionTitlesPack.Runtime.asmdef 생성 |
| Easy Save 3 | ES3 타입이 글로벌 어셈블리 | JsonUtility로 대체 |

### VFXManager 5번째 시도 성공
RenderTexture, 오버레이 카메라, 인씬 Canvas, 전용 카메라 → URP Camera Stacking

## 수정 파일 목록

| 파일 | 변경 |
|------|------|
| `Combat/Turn/TurnManager.cs` | OnSkillApplied 이벤트 추가 |
| `Editor/DataGenerator.Content.cs` | AudioPalette 14개 매핑 + GetItemIconPath() |
| `UI/AudioManager.cs` | 14개 편의 메서드 + AudioListener 자동 생성 |
| `Combat/BattleSceneSetup.cs` | 스킬 사운드 라우팅 + BattleTitleManager 연동 |
| `Reward/ItemData.cs` | _icon 필드 + Icon 프로퍼티 |
| `UI/Shop/ShopItemSlot.cs` | _iconImage 추가 |
| `UI/Reward/RewardCard.cs` | _iconImage 추가 |
| `UI/VFXManager.cs` | URP Camera Stacking 전면 재작성 |
| `UI/Battle/BattleTitleManager.cs` | 신규 |
| `Map/SaveManager.cs` | 신규 (JsonUtility 기반) |
| `Map/GameRunState.cs` | RestoreBonusAP() 추가 |
| `TeamLog.Runtime.asmdef` | URP + MTP 참조 추가 |
| `MotionTitlesPack.Runtime.asmdef` | 신규 |
| `03.Data/Tables/CharacterTable.csv` | 밸런싱 |
| `03.Data/Tables/SkillTable.csv` | 밸런싱 |

## 컴파일 결과
0 에러, 14 경고 (TMP enableWordWrapping obsolete, unused field)
